name: Github Actions Push
run-name: ${{ github.actor }} is updating .yml file
on: [push]
jobs:
  Explore-GitHub-Actions:
    runs-on: ubuntu-latest
    steps:
      - run: echo "Updating environment.yml"
      - run: |
          conda env export -f environment.yml
